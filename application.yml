management:
  security:
    enabled: false

security:
  basic:
    enabled: false

spring:
  cloud:
    services:
      registrationMethod: direct

logging:
  level:
    com:
      xoriant:
        demo: INFO
